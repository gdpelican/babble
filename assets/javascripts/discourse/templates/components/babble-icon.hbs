{{#unless noTopicAvailable}}
  {{#header-dropdown iconId="babble-notifications"
                     icon="bullhorn"
                     toggleVisible=babbleVisible
                     loginAction="showLogin"
                     title="babble.title"}}
  {{/header-dropdown}}
  {{#if unreadCount}}
    <a href class='badge-notification unread-notifications'>{{unreadCount}}</a>
  {{/if}}
{{/unless}}
