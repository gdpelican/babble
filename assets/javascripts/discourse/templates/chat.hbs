<div class="chat fullpage">
  {{#if model.id}}
    <div class="chat-pane">
      {{chat-container
        topic=model
        application=application
        showCreateAccount="showCreateAccount"
        showLogin="showLogin"
        nearPost=nearPost}}
    </div>
  {{else}}
    {{loading-spinner}}
  {{/if}}
</div>
